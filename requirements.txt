# =========================================================================
# Cricket Analytics — Python backend dependencies
# Pinned to the versions verified working in the project's environment.
# Install with:  pip install -r requirements.txt
# =========================================================================

# ── FastAPI backend (backend/main.py — the production API) ──────────────
fastapi==0.141.1
uvicorn[standard]==0.52.1
starlette==1.5.0
pydantic==2.12.4
pydantic-settings==2.15.0
python-dotenv==1.2.1
python-multipart==0.0.32        # file uploads (historical dataset upload)
websockets==17.0.1              # live match WebSocket streaming (ws_manager)

# ── Authentication / security ───────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT signing/verification
bcrypt==4.2.1                     # password hashing
google-auth==2.35.0               # Google Sign-In (OAuth) verification
email-validator==2.2.0            # email field validation
cuid2==2.0.1                      # collision-resistant IDs

# ── HTTP client (RapidAPI live-cricket feed) ────────────────────────────
requests==2.32.3

# ── Machine learning / data ─────────────────────────────────────────────
scikit-learn==1.9.0
catboost==1.2.10
joblib==1.5.3
pandas==2.2.3
numpy==2.1.3
lightgbm==4.6.0                 # win-prediction model + simulation engine

# ── Legacy Flask app (app.py — Phase 1/2 dashboards) ────────────────────
flask==3.1.2
plotly==5.24.1

# ── Testing (optional; needed only to run the test suite) ───────────────
# pytest==9.1.1
# httpx==0.27.2                  # FastAPI TestClient dependency
